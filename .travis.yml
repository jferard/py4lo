# py4lo
language: python

python:
  - "3.4"

# command to run tests and install dependencies
install:
  - pip install codecov
  - pip install -r requirements.txt

# script
script:
  - py.test

after_success:
  - codecov
