# py4lo
language: python
python:
  - "3.4"
# command to install dependencies
install: "pip install -r requirements.txt"

# command to run tests
install:
  - pip install codecov
  - pip install pytest

script:
  - pytest tests.py

after_success:
  - codecov
